version: "2"

build:
  os: "ubuntu-latest"
  apt_packages: 
    - libmagic1
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
